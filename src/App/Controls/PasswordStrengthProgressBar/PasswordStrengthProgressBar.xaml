<?xml version="1.0" encoding="UTF-8" ?>
<StackLayout
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Bit.App.Controls"
    x:Name="_passwordStrengthProgressBar"
    x:DataType="controls:PasswordStrengthViewModel"
    x:Class="Bit.App.Controls.PasswordStrengthProgressBar"
    StyleClass="box">

    <ProgressBar
        x:Name="_passwordBar"
        ProgressColor="{Binding PasswordColor}"
        ScaleY="2" />

    <Label
        x:Name="_passwordStatus"
        Text="{Binding PasswordStatus}"
        TextColor="{Binding PasswordColor}"
        StyleClass="box-footer-label" />

</StackLayout>

