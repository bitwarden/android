namespace Bit.App.Enums
{
    public enum CipherType : short
    {
        // Folder deprecated
        //Folder = 0,
        Login = 1,
        SecureNote = 2,
        Card = 3,
        Identity = 4
    }
}
