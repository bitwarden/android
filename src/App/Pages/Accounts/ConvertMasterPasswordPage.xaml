<?xml version="1.0" encoding="UTF-8" ?>
<pages:BaseContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Bit.App.Pages.ConvertMasterPasswordPage"
    xmlns:pages="clr-namespace:Bit.App.Pages"
    xmlns:u="clr-namespace:Bit.App.Utilities"
    x:DataType="pages:ConvertMasterPasswordPageViewModel"
    Title="{Binding PageTitle}">

    <ContentPage.BindingContext>
        <pages:ConvertMasterPasswordPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{u:I18n Close}" Clicked="Close_Clicked" Order="Primary" Priority="-1" />
    </ContentPage.ToolbarItems>

        <StackLayout Spacing="20"
                     Padding="10, 5">
            <StackLayout Padding="30">
                <Label Text="{u:I18n ConvertMasterPasswordWarning}"
                   HorizontalTextAlignment="Center"/>
            <Label Text="{u:I18n ConvertMasterPasswordLeave}"
                   HorizontalTextAlignment="Center"/>
            </StackLayout>
            <StackLayout Spacing="5">
                <Button Text="{u:I18n Continue}" 
                        StyleClass="btn-primary"
                        Clicked="Continue_Clicked" />
                <Button Text="{u:I18n LeaveOrganization}"
                        Clicked="LeaveOrg_Clicked" />
            </StackLayout>
        </StackLayout>


</pages:BaseContentPage>
